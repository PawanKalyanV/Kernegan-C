

#include<stdio.h>

int main()
{
  int c, n=0, count, end_c;

 while (( c = getchar() ) != EOF )
  
      if ( c == ' ' )
       {
	 {
          if ( end_c == ' ' )
           n++;
         }
       }
       printf("%d",n);
       end_c = c;
}
